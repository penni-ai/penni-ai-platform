# Firebase Cloud Functions environment variables template
# Copy this file to .env for local emulator tests. In production set
# secrets/config via `firebase functions:secrets:set` or runtime config.
# NOTE: The reranker stage has been retired, so no DEEPINFRA_RERANKER_*
# variables are required anymore.

############################################################
# Firebase / Deployment (optional overrides)
############################################################
# Defaults to us-central1 when unset.
FUNCTION_REGION=us-central1

############################################################
# Core API secrets (store via Firebase Secrets in production)
############################################################
OPENAI_API_KEY=your-openai-key
DEEPINFRA_API_KEY=your-deepinfra-key
BRIGHTDATA_API_KEY=your-brightdata-key
WEAVIATE_API_KEY=your-weaviate-key

############################################################
# Weaviate search configuration (required)
############################################################
# Hosted Weaviate cluster URL (https, no trailing slash required).
WEAVIATE_URL=https://your-cluster.weaviate.network
# Collection name containing influencer profiles.
WEAVIATE_COLLECTION_NAME=influencer_profiles
# Search tuning knobs (optional overrides, defaults shown).
WEAVIATE_TOP_N=1000
WEAVIATE_RESULTS_PER_QUERY=500
# Comma-separated alpha values between 0.0 and 1.0 (no spaces needed).
WEAVIATE_ALPHA_VALUES=0.2,0.5,0.8

############################################################
# DeepInfra embedding configuration
############################################################
# Base URL + OpenAI-compatible paths. Override only if DeepInfra changes them.
DEEPINFRA_BASE_URL=https://api.deepinfra.com
DEEPINFRA_EMBED_PATH=/v1/openai
DEEPINFRA_INFERENCE_PATH=/v1/inference
# Embedding model name passed to DeepInfra. No separate dimension variable needed.
DEEPINFRA_EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B

############################################################
# BrightData enrichment configuration
############################################################
# At least one dataset ID must be present for the BrightData stage to run.
BRIGHTDATA_INSTAGRAM_DATASET_ID=
BRIGHTDATA_TIKTOK_DATASET_ID=
BRIGHTDATA_BASE_URL=https://api.brightdata.com/datasets/v3
BRIGHTDATA_POLL_INTERVAL=30
BRIGHTDATA_MAX_URLS=50

############################################################
# Guidance
############################################################
# 1. Keep secrets (API keys) out of git; store them with Firebase Secrets Manager.
# 2. The embedding model can remain at its default unless you migrate Weaviate to a
#    schema that requires a different model. Embedding dimensions are controlled by
#    the model/schema, so no extra env is necessary.
# 3. BrightData variables are only required if you run the enrichment stage.
# 4. Update values here for local testing only; production should rely on Firebase config.
