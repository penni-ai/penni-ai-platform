# GCP Project ID for Vertex AI
# This should match your Firebase/GCP project ID
GCP_PROJECT_ID=penni-ai-platform

# Optional: Google Cloud Authentication
# If not using Application Default Credentials (gcloud auth application-default login),
# you can specify a service account key file path
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Optional: GCP Region for Vertex AI
# Default is 'global' but you can specify a specific region like 'us-central1'
# GCP_REGION=global

